apiVersion: widgets.templates.krateo.io/v1beta1
kind: PageHeader
metadata:
  name: page-service-register-header
  namespace: '{{ include "page.tierNamespace" (dict "ctx" . "tier" "common") }}'
spec:
  resourcesRefs:
    items: []
  widgetData:
    title: Service register
