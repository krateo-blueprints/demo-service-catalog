apiVersion: widgets.templates.krateo.io/v1beta1
kind: Flex
metadata:
  annotations:
    krateo.io/allowed-resources: derived
    krateo.io/nav-label: Pod sizing
    krateo.io/nav-order: '100'
    krateo.io/nav-path: /pod-sizing-v1
  name: page-pod-sizing-v1
  namespace: '{{ include "page.tierNamespace" (dict "ctx" . "tier" "common") }}'
spec:
  resourcesRefs:
    items:
      - apiVersion: widgets.templates.krateo.io/v1beta1
        id: page-pod-sizing-v1-row
        name: page-pod-sizing-v1-row
        namespace: krateo-system
        resource: rows
        verb: GET
      - apiVersion: widgets.templates.krateo.io/v1beta1
        id: page-pod-sizing-v1-header
        name: page-pod-sizing-v1-header
        namespace: '{{ include "page.tierNamespace" (dict "ctx" . "tier" "common") }}'
        resource: pageheaders
        verb: GET
      - apiVersion: widgets.templates.krateo.io/v1beta1
        id: pod-sizing
        name: pod-sizing
        namespace: krateo-system
        resource: tables
        verb: GET
  widgetData:
    allowedResources:
      - rows
      - pageheaders
      - tables
    gap: middle
    items:
      - resourceRefId: page-pod-sizing-v1-row
      - resourceRefId: page-pod-sizing-v1-header
      - resourceRefId: pod-sizing
    vertical: true
