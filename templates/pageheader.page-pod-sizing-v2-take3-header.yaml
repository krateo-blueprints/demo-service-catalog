apiVersion: widgets.templates.krateo.io/v1beta1
kind: PageHeader
metadata:
  name: page-pod-sizing-v2-take3-header
  namespace: '{{ include "page.tierNamespace" (dict "ctx" . "tier" "common") }}'
spec:
  resourcesRefs:
    items: []
  widgetData:
    title: Pod sizing
